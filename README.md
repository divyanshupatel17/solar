
  # Solar Token System Platform (Community)

  This is a code bundle for Solar Token System Platform (Community). The original project is available at https://www.figma.com/design/kmqVeDhlcVNowu6hGPxf69/Solar-Token-System-Platform--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  